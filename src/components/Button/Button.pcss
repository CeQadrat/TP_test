.button {
  width: 100%;
  padding: 12px;
  font-size: 12px;
  text-transform: uppercase;
  color: #ffffff;
  border: none;
  border-radius: 4px;
  background: #f5a624;
  cursor: pointer;
}

.button:hover {
  background: #f6ae3a;
}

.button:active {
  background: #f19a0b;
}
